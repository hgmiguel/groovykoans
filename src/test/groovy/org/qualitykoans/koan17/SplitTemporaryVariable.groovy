package org.qualitykoans.koan17

class SplitTemporaryVariable {

  double getSize(height, width) {
    double temp = 2 * (height + width);
    //do some things ...
    temp = height * width;

    temp
  }
}
